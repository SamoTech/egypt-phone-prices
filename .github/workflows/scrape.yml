# Scraping is now handled by Vercel Cron Jobs (vercel.json).
# This file is intentionally left as a no-op placeholder.
# The cron fires daily at 01:00 UTC (03:00 Cairo) via:
#   POST /api/cron/scrape  →  FastAPI /admin/trigger-scrape
name: '[DISABLED] Daily Scrape'
on:
  workflow_dispatch:  # manual only — disabled schedule
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Scraping is handled by Vercel Cron. See vercel.json.'
