# Slim requirements for Vercel serverless (no Celery/worker deps)
# Vercel uses this file when VERCEL_REQUIREMENTS_FILE is set,
# otherwise falls back to requirements.txt.

# Core
fastapi==0.115.6
mangum==0.19.0
pydantic==2.10.3
pydantic-settings==2.7.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Scraping (needed for on-demand endpoints)
httpx==0.28.1
rapidfuzz==3.11.0
tenacity==9.0.0
loguru==0.7.3
python-dotenv==1.0.1
cachetools==5.5.0
