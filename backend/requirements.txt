# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1
mangum==0.19.0
pydantic==2.10.3
pydantic-settings==2.7.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Scraping
httpx==0.28.1
fake-useragent==2.0.3
tenacity==9.0.0
rapidfuzz==3.11.0

# Cache / Utils
cachetools==5.5.0
python-dotenv==1.0.1
loguru==0.7.3

# Note: scrapling, celery, redis removed — too heavy for Vercel serverless.
# Scraping runs via GitHub Actions or local CLI: python -m app.tasks.run_scrape
